{
  "name": "spike-opfs",
  "private": true,
  "type": "module",
  "description": "Spike 3 (T-B1.3) — OPFS persistence via WASI preopen; Playwright headed/watch optional",
  "scripts": {
    "test": "playwright test",
    "test:watch": "SPIKE_OPFS_WATCH=1 playwright test --headed",
    "test:headed": "SPIKE_OPFS_HEADED=1 playwright test --headed"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.25.2",
    "@bytecodealliance/preview2-shim": "^0.19.0",
    "@playwright/test": "^1.54.1",
    "vite": "^7.0.5"
  }
}
