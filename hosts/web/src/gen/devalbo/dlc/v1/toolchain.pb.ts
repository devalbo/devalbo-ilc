// @generated by protoc-gen-es-lite unknown with parameter "target=ts"
// @generated from file devalbo/dlc/v1/toolchain.proto (package devalbo.dlc.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import type { MessageType } from "@aptre/protobuf-es-lite/message";
import { createEmptyMessageType, createMessageType } from "@aptre/protobuf-es-lite/message";
import { ScalarType } from "@aptre/protobuf-es-lite/scalar";
import type { PartialFieldInfo } from "@aptre/protobuf-es-lite/field";

export const protobufPackage = "devalbo.dlc.v1";

/**
 * `dlc gen` takes no arguments — the project's `dlc.toml` and `.proto` files say
 * everything. Empty because there is nothing to declare, not because it is
 * unfinished.
 *
 * @generated from message devalbo.dlc.v1.GenRequest
 */
export interface GenRequest {

};

export const GenRequest: MessageType<GenRequest> = /* @__PURE__ */ createEmptyMessageType<GenRequest>("devalbo.dlc.v1.GenRequest", true);

/**
 * @generated from message devalbo.dlc.v1.GenResponse
 */
export interface GenResponse {

};

export const GenResponse: MessageType<GenResponse> = /* @__PURE__ */ createEmptyMessageType<GenResponse>("devalbo.dlc.v1.GenResponse", true);

/**
 * `dlc build <tier> [--out …] [--web-out …] [--entry …]`
 *
 * @generated from message devalbo.dlc.v1.BuildRequest
 */
export interface BuildRequest {
  /**
   * @generated from field: string tier = 1;
   */
  tier?: string;
  /**
   * NO DEFAULTS on out/web_out, deliberately. Precedence is built-in default,
   * then `dlc.toml`'s [tiers.<tier>], then the flag — so the host has to be able
   * to tell "not given" from "given", and a declared default here would make
   * every invocation look explicit and silently clobber the manifest.
   *
   * proto3's empty string is exactly that distinction, which is why these are
   * plain strings with no default rather than a wrapper type.
   *
   * @generated from field: string out = 2;
   */
  out?: string;
  /**
   * @generated from field: string web_out = 3;
   */
  webOut?: string;
  /**
   * A constant default, not a manifest-derived one, so declaring it here is safe.
   *
   * @generated from field: string entry = 4;
   */
  entry?: string;

};

export const BuildRequest: MessageType<BuildRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.dlc.v1.BuildRequest",
    fields: [
        { no: 1, name: "tier", kind: "scalar", T: ScalarType.STRING },
        { no: 2, name: "out", kind: "scalar", T: ScalarType.STRING },
        { no: 3, name: "web_out", kind: "scalar", T: ScalarType.STRING },
        { no: 4, name: "entry", kind: "scalar", T: ScalarType.STRING },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.dlc.v1.BuildResponse
 */
export interface BuildResponse {

};

export const BuildResponse: MessageType<BuildResponse> = /* @__PURE__ */ createEmptyMessageType<BuildResponse>("devalbo.dlc.v1.BuildResponse", true);

/**
 * `dlc run <tier> [args…]` — the two-phase launch (§5.5) for whichever tier can
 * be launched from a developer machine.
 *
 * NOT EVERY TIER CAN RUN, and the default answer is "not supported". A tier is a
 * host binding, and only some bindings are things `dlc` can start: it can exec a
 * native binary and it can serve a web tier, but it cannot flash a board or push
 * a firmware image, and pretending otherwise would be worse than refusing. A
 * declared-but-unlaunchable tier gets a message naming itself.
 *
 * @generated from message devalbo.dlc.v1.RunRequest
 */
export interface RunRequest {
  /**
   * @generated from field: string tier = 1;
   */
  tier?: string;
  /**
   * Everything after the tier goes to the launched program, which is why this is
   * a REPEATED positional and therefore last: `dlc run native state 3` runs the
   * CLI with `state 3`.
   *
   * The web tier has nothing to forward these to, and says so rather than
   * ignoring them — a silently dropped argument is the kind of thing you only
   * notice when the output is wrong.
   *
   * @generated from field: repeated string args = 2;
   */
  args?: string[];
  /**
   * Serve only; do not open a browser. The default is to open one, because
   * `dlc run web` with nothing appearing looks broken.
   *
   * @generated from field: bool no_open = 3;
   */
  noOpen?: boolean;

};

export const RunRequest: MessageType<RunRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.dlc.v1.RunRequest",
    fields: [
        { no: 1, name: "tier", kind: "scalar", T: ScalarType.STRING },
        { no: 2, name: "args", kind: "scalar", T: ScalarType.STRING, repeated: true },
        { no: 3, name: "no_open", kind: "scalar", T: ScalarType.BOOL },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.dlc.v1.RunResponse
 */
export interface RunResponse {

};

export const RunResponse: MessageType<RunResponse> = /* @__PURE__ */ createEmptyMessageType<RunResponse>("devalbo.dlc.v1.RunResponse", true);

