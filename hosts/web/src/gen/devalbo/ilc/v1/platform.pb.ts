// @generated by protoc-gen-es-lite unknown with parameter "target=ts"
// @generated from file devalbo/ilc/v1/platform.proto (package devalbo.ilc.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { createEnumType } from "@aptre/protobuf-es-lite/enum";
import type { MessageType } from "@aptre/protobuf-es-lite/message";
import { createEmptyMessageType, createMessageType } from "@aptre/protobuf-es-lite/message";
import { ScalarType } from "@aptre/protobuf-es-lite/scalar";
import type { PartialFieldInfo } from "@aptre/protobuf-es-lite/field";

export const protobufPackage = "devalbo.ilc.v1";

/**
 * Whether a capability is there.
 *
 * Three states rather than a bool, because UNSPECIFIED is not ABSENT: the first
 * means nobody said, the second means somebody said no. They happen to lead to
 * the same conservative behaviour (an app takes its fallback path either way),
 * but conflating them would make "the host forgot to send a manifest"
 * indistinguishable from "this host has no filesystem" — and only one of those
 * is a bug worth surfacing.
 *
 * @generated from enum devalbo.ilc.v1.Availability
 */
export enum Availability {
  /**
   * nobody said; treated as absent
   *
   * @generated from enum value: AVAILABILITY_UNSPECIFIED = 0;
   */
  UNSPECIFIED = 0,

  /**
   * @generated from enum value: AVAILABILITY_ABSENT = 1;
   */
  ABSENT = 1,

  /**
   * @generated from enum value: AVAILABILITY_PRESENT = 2;
   */
  PRESENT = 2,
}

export const Availability_Enum = /* @__PURE__ */ createEnumType("devalbo.ilc.v1.Availability", [
  [0, "AVAILABILITY_UNSPECIFIED"],
  [1, "AVAILABILITY_ABSENT"],
  [2, "AVAILABILITY_PRESENT"],
]);

/**
 * What KIND of root the host granted. Not cosmetic: an app deciding what to
 * cache cares whether its store survives the tab closing.
 *
 * @generated from enum devalbo.ilc.v1.FilesystemKind
 */
export enum FilesystemKind {
  /**
   * @generated from enum value: FILESYSTEM_KIND_UNSPECIFIED = 0;
   */
  UNSPECIFIED = 0,

  /**
   * the user's project — `dlc` itself (§3·5)
   *
   * @generated from enum value: FILESYSTEM_KIND_CWD = 1;
   */
  CWD = 1,

  /**
   * ./.<app>/ — the convention for everyone else
   *
   * @generated from enum value: FILESYSTEM_KIND_APP_DIR = 2;
   */
  APP_DIR = 2,

  /**
   * the browser's origin-private filesystem
   *
   * @generated from enum value: FILESYSTEM_KIND_OPFS = 3;
   */
  OPFS = 3,
}

export const FilesystemKind_Enum = /* @__PURE__ */ createEnumType("devalbo.ilc.v1.FilesystemKind", [
  [0, "FILESYSTEM_KIND_UNSPECIFIED"],
  [1, "FILESYSTEM_KIND_CWD"],
  [2, "FILESYSTEM_KIND_APP_DIR"],
  [3, "FILESYSTEM_KIND_OPFS"],
]);

/**
 * Whether this root belongs to ONE person, or is shared with others.
 *
 * Separate from FilesystemKind rather than a fourth value of it, because it is
 * an orthogonal dimension: `kind` already mixes WHERE the root is (cwd, app dir)
 * with WHAT backs it (OPFS), and a per-user OPFS subtree is genuinely both OPFS
 * and per-user. An enum can only say one thing.
 *
 * Three states for the same reason Availability has three: "nobody said" is not
 * "somebody said it is shared", and only the second is a claim.
 *
 * @generated from enum devalbo.ilc.v1.Isolation
 */
export enum Isolation {
  /**
   * nobody said; treated as SHARED
   *
   * @generated from enum value: ISOLATION_UNSPECIFIED = 0;
   */
  UNSPECIFIED = 0,

  /**
   * other people may see this store
   *
   * @generated from enum value: ISOLATION_SHARED = 1;
   */
  SHARED = 1,

  /**
   * this root was granted for one person
   *
   * @generated from enum value: ISOLATION_PER_USER = 2;
   */
  PER_USER = 2,
}

export const Isolation_Enum = /* @__PURE__ */ createEnumType("devalbo.ilc.v1.Isolation", [
  [0, "ISOLATION_UNSPECIFIED"],
  [1, "ISOLATION_SHARED"],
  [2, "ISOLATION_PER_USER"],
]);

/**
 * Filesystem export/import — the first-class platform primitive (§7.3). An
 * app's whole state is a filesystem tree, so one bundle moves it between the
 * terminal, the browser, and an embedded device.
 *
 * @generated from enum devalbo.ilc.v1.BundleFormat
 */
export enum BundleFormat {
  /**
   * treat as BFT (the default)
   *
   * @generated from enum value: BUNDLE_FORMAT_UNSPECIFIED = 0;
   */
  UNSPECIFIED = 0,

  /**
   * single human-readable JSON
   *
   * @generated from enum value: BUNDLE_FORMAT_BFT = 1;
   */
  BFT = 1,

  /**
   * @generated from enum value: BUNDLE_FORMAT_ZIP = 2;
   */
  ZIP = 2,

  /**
   * @generated from enum value: BUNDLE_FORMAT_PROTO = 3;
   */
  PROTO = 3,
}

export const BundleFormat_Enum = /* @__PURE__ */ createEnumType("devalbo.ilc.v1.BundleFormat", [
  [0, "BUNDLE_FORMAT_UNSPECIFIED"],
  [1, "BUNDLE_FORMAT_BFT"],
  [2, "BUNDLE_FORMAT_ZIP"],
  [3, "BUNDLE_FORMAT_PROTO"],
]);

/**
 * How an import treats what is already on disk. Explicit because the default
 * (merge) silently cannot express a deletion: dropping a file from a bundle and
 * re-importing leaves the old file in place.
 *
 * @generated from enum devalbo.ilc.v1.ImportMode
 */
export enum ImportMode {
  /**
   * treat as MERGE
   *
   * @generated from enum value: IMPORT_MODE_UNSPECIFIED = 0;
   */
  UNSPECIFIED = 0,

  /**
   * write the bundle over whatever is there
   *
   * @generated from enum value: IMPORT_MODE_MERGE = 1;
   */
  MERGE = 1,

  /**
   * clear the destination first, then write
   *
   * @generated from enum value: IMPORT_MODE_REPLACE = 2;
   */
  REPLACE = 2,
}

export const ImportMode_Enum = /* @__PURE__ */ createEnumType("devalbo.ilc.v1.ImportMode", [
  [0, "IMPORT_MODE_UNSPECIFIED"],
  [1, "IMPORT_MODE_MERGE"],
  [2, "IMPORT_MODE_REPLACE"],
]);

/**
 * @generated from message devalbo.ilc.v1.VersionRequest
 */
export interface VersionRequest {

};

export const VersionRequest: MessageType<VersionRequest> = /* @__PURE__ */ createEmptyMessageType<VersionRequest>("devalbo.ilc.v1.VersionRequest", true);

/**
 * @generated from message devalbo.ilc.v1.VersionResponse
 */
export interface VersionResponse {
  /**
   * @generated from field: string version = 1;
   */
  version?: string;

};

export const VersionResponse: MessageType<VersionResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.VersionResponse",
    fields: [
        { no: 1, name: "version", kind: "scalar", T: ScalarType.STRING },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.Filesystem
 */
export interface Filesystem {
  /**
   * @generated from field: devalbo.ilc.v1.Availability availability = 1;
   */
  availability?: Availability;
  /**
   * @generated from field: devalbo.ilc.v1.FilesystemKind kind = 2;
   */
  kind?: FilesystemKind;
  /**
   * Ephemeral means the store does not survive the process/tab. An app may
   * still write; it should not promise the user durability.
   *
   * @generated from field: bool ephemeral = 3;
   */
  ephemeral?: boolean;
  /**
   * Whether the host isolated this store per user (§3·5).
   *
   * WHO READS THIS, since no app in this repo does yet: an app that holds
   * PRIVATE data and needs to know whether privacy is its problem. If the host
   * granted a per-user root, everything the app can see belongs to one person
   * and it may be naive. If it did not, the app is responsible for access
   * control, and an app that assumed otherwise leaks — silently, and only
   * discoverably by leaking.
   *
   * That asymmetry is why this field exists ahead of its first consumer, where
   * ENVIRONMENT-PLAN.md D6 would normally say to wait: an unused field costs one
   * field, and a missing one costs someone else's data.
   *
   * UNSPECIFIED IS SAFE, which is what makes it addable without touching a
   * single existing host: no claim reads as "not isolated", so an app that
   * requires privacy refuses to run rather than assuming it has it. A host that
   * forgets to declare isolation therefore fails LOUDLY instead of leaking.
   *
   * NOT A SECURITY BOUNDARY. This is the host's word. A host that grants a
   * shared root and reports PER_USER is lying, and nothing engine-side can
   * detect it — see AGENTS.md §3·5. It lets an honest host tell an app what it
   * is getting; it does not enforce anything.
   *
   * @generated from field: devalbo.ilc.v1.Isolation isolation = 4;
   */
  isolation?: Isolation;

};

export const Filesystem: MessageType<Filesystem> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.Filesystem",
    fields: [
        { no: 1, name: "availability", kind: "enum", T: Availability_Enum },
        { no: 2, name: "kind", kind: "enum", T: FilesystemKind_Enum },
        { no: 3, name: "ephemeral", kind: "scalar", T: ScalarType.BOOL },
        { no: 4, name: "isolation", kind: "enum", T: Isolation_Enum },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * Environment is what the host can do, as of `revision`.
 *
 * Deliberately small (plan D6): a field here is a branch somewhere, and a
 * capability nothing reads yet is a branch nothing tests. Display may never land
 * at all now that Decision 34 made the semantic render path the default. Events
 * are absent BY RULE, not by omission — emitting carries nothing back, so its
 * absence is unobservable and Decision 33 forbids an app branching on it.
 *
 * @generated from message devalbo.ilc.v1.Environment
 */
export interface Environment {
  /**
   * Monotonic, host-assigned, and REQUIRED to be non-zero — the engine rejects
   * 0 rather than reading it as "the first one", so a host that forgot cannot
   * masquerade as a host that is up to date.
   *
   * Its reader is registration: SetEnvironment re-runs capability registration
   * and churns the command surface, so an unchanged revision is how the engine
   * knows to do nothing. A slot can also tell whether what it rendered from is
   * still current.
   *
   * @generated from field: uint32 revision = 1;
   */
  revision?: number;
  /**
   * @generated from field: devalbo.ilc.v1.Filesystem filesystem = 2;
   */
  filesystem?: Filesystem;

};

export const Environment: MessageType<Environment> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.Environment",
    fields: [
        { no: 1, name: "revision", kind: "scalar", T: ScalarType.UINT32 },
        { no: 2, name: "filesystem", kind: "message", T: () => Filesystem },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.GetCommandSurfaceRequest
 */
export interface GetCommandSurfaceRequest {

};

export const GetCommandSurfaceRequest: MessageType<GetCommandSurfaceRequest> = /* @__PURE__ */ createEmptyMessageType<GetCommandSurfaceRequest>("devalbo.ilc.v1.GetCommandSurfaceRequest", true);

/**
 * @generated from message devalbo.ilc.v1.GetCommandSurfaceResponse
 */
export interface GetCommandSurfaceResponse {
  /**
   * Every registered method_id, ASCENDING. Sorted because map iteration order
   * is unspecified in Go, and an unsorted answer would make the parity diff
   * fail at random — a flake that would read as a real divergence.
   *
   * Ids rather than names: the id is the wire and the name is cosmetic
   * (AGENTS.md §3b), and the caller already has the names — it holds the
   * generated clispec and is asking which of those are live.
   *
   * @generated from field: repeated uint32 method_ids = 1;
   */
  methodIds?: number[];

};

export const GetCommandSurfaceResponse: MessageType<GetCommandSurfaceResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.GetCommandSurfaceResponse",
    fields: [
        { no: 1, name: "method_ids", kind: "scalar", T: ScalarType.UINT32, repeated: true },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.SetEnvironmentRequest
 */
export interface SetEnvironmentRequest {
  /**
   * @generated from field: devalbo.ilc.v1.Environment environment = 1;
   */
  environment?: Environment;

};

export const SetEnvironmentRequest: MessageType<SetEnvironmentRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.SetEnvironmentRequest",
    fields: [
        { no: 1, name: "environment", kind: "message", T: () => Environment },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.SetEnvironmentResponse
 */
export interface SetEnvironmentResponse {
  /**
   * Whether this manifest actually changed anything. False means the revision
   * matched what was already in force and registration was left alone.
   *
   * @generated from field: bool applied = 1;
   */
  applied?: boolean;

};

export const SetEnvironmentResponse: MessageType<SetEnvironmentResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.SetEnvironmentResponse",
    fields: [
        { no: 1, name: "applied", kind: "scalar", T: ScalarType.BOOL },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.ExportFsRequest
 */
export interface ExportFsRequest {
  /**
   * @generated from field: string prefix = 1;
   */
  prefix?: string;
  /**
   * @generated from field: devalbo.ilc.v1.BundleFormat format = 2;
   */
  format?: BundleFormat;

};

export const ExportFsRequest: MessageType<ExportFsRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ExportFsRequest",
    fields: [
        { no: 1, name: "prefix", kind: "scalar", T: ScalarType.STRING },
        { no: 2, name: "format", kind: "enum", T: BundleFormat_Enum },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.ExportFsResponse
 */
export interface ExportFsResponse {
  /**
   * @generated from field: bytes bundle = 1;
   */
  bundle?: Uint8Array;

};

export const ExportFsResponse: MessageType<ExportFsResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ExportFsResponse",
    fields: [
        { no: 1, name: "bundle", kind: "scalar", T: ScalarType.BYTES },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.ImportFsRequest
 */
export interface ImportFsRequest {
  /**
   * @generated from field: bytes bundle = 1;
   */
  bundle?: Uint8Array;
  /**
   * @generated from field: string prefix = 2;
   */
  prefix?: string;
  /**
   * @generated from field: devalbo.ilc.v1.ImportMode mode = 3;
   */
  mode?: ImportMode;

};

export const ImportFsRequest: MessageType<ImportFsRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ImportFsRequest",
    fields: [
        { no: 1, name: "bundle", kind: "scalar", T: ScalarType.BYTES },
        { no: 2, name: "prefix", kind: "scalar", T: ScalarType.STRING },
        { no: 3, name: "mode", kind: "enum", T: ImportMode_Enum },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.ImportFsResponse
 */
export interface ImportFsResponse {
  /**
   * @generated from field: repeated string files = 1;
   */
  files?: string[];

};

export const ImportFsResponse: MessageType<ImportFsResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ImportFsResponse",
    fields: [
        { no: 1, name: "files", kind: "scalar", T: ScalarType.STRING, repeated: true },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * reset-fs — delete a subtree (§7.3). The counterpart that makes "replace"
 * expressible on its own, and the only way to re-scaffold over an existing tree.
 *
 * @generated from message devalbo.ilc.v1.ResetFsRequest
 */
export interface ResetFsRequest {
  /**
   * @generated from field: string prefix = 1;
   */
  prefix?: string;

};

export const ResetFsRequest: MessageType<ResetFsRequest> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ResetFsRequest",
    fields: [
        { no: 1, name: "prefix", kind: "scalar", T: ScalarType.STRING },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.ResetFsResponse
 */
export interface ResetFsResponse {
  /**
   * top-level entries that were deleted
   *
   * @generated from field: repeated string removed = 1;
   */
  removed?: string[];

};

export const ResetFsResponse: MessageType<ResetFsResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.ResetFsResponse",
    fields: [
        { no: 1, name: "removed", kind: "scalar", T: ScalarType.STRING, repeated: true },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.RebuildIndexRequest
 */
export interface RebuildIndexRequest {

};

export const RebuildIndexRequest: MessageType<RebuildIndexRequest> = /* @__PURE__ */ createEmptyMessageType<RebuildIndexRequest>("devalbo.ilc.v1.RebuildIndexRequest", true);

/**
 * @generated from message devalbo.ilc.v1.RebuildIndexResponse
 */
export interface RebuildIndexResponse {
  /**
   * How many entries the rebuilt index holds. The one number worth returning:
   * it makes "rebuild did nothing" distinguishable from "rebuild ran and the
   * collection is empty", which is the difference between a broken rebuilder and
   * an empty store.
   *
   * @generated from field: uint32 entries = 1;
   */
  entries?: number;

};

export const RebuildIndexResponse: MessageType<RebuildIndexResponse> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.RebuildIndexResponse",
    fields: [
        { no: 1, name: "entries", kind: "scalar", T: ScalarType.UINT32 },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * DataChangedEvent is the payload of the platform's `ilc.data-changed` topic:
 * "the filesystem under this prefix changed, re-read what you care about".
 *
 * Deliberately NOT a diff. A precise change list would be a second source of
 * truth that a subscriber could act on WITHOUT re-reading the files — and the
 * files are the truth (§7.1). Telling a UI only that something moved keeps the
 * filesystem authoritative and keeps this message from growing into a protocol.
 * EnvironmentChangedEvent announces that the facts moved (§6.4a).
 *
 * Emitted only when a manifest actually APPLIES — a re-send of the revision
 * already in force changes nothing, and announcing it would have every listener
 * re-read for no reason.
 *
 * Why an event and not a return value: the host that sent the manifest already
 * knows. This is for everyone ELSE — a command inspector showing which verbs
 * this host provides, a slot rendering a capability-dependent control. They did
 * not make the call and have no other way to learn the answer changed.
 *
 * Carries the revision and nothing else, for the same reason DataChangedEvent
 * is not a diff: a listener that acted on the payload would be treating the
 * event as the source of truth instead of re-reading. Ask the engine.
 *
 * @generated from message devalbo.ilc.v1.EnvironmentChangedEvent
 */
export interface EnvironmentChangedEvent {
  /**
   * The revision now in force. Enough for a listener to tell whether it has
   * already caught up, and useless for anything else — deliberately.
   *
   * @generated from field: uint32 revision = 1;
   */
  revision?: number;

};

export const EnvironmentChangedEvent: MessageType<EnvironmentChangedEvent> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.EnvironmentChangedEvent",
    fields: [
        { no: 1, name: "revision", kind: "scalar", T: ScalarType.UINT32 },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

/**
 * @generated from message devalbo.ilc.v1.DataChangedEvent
 */
export interface DataChangedEvent {
  /**
   * Root-relative subtree that changed. Empty means the whole root.
   *
   * @generated from field: string prefix = 1;
   */
  prefix?: string;
  /**
   * The command that caused it, as a method_id — enough for a subscriber to
   * ignore its own writes without the platform having to model identity.
   *
   * @generated from field: uint32 method = 2;
   */
  method?: number;

};

export const DataChangedEvent: MessageType<DataChangedEvent> = /* @__PURE__ */ createMessageType({
    typeName: "devalbo.ilc.v1.DataChangedEvent",
    fields: [
        { no: 1, name: "prefix", kind: "scalar", T: ScalarType.STRING },
        { no: 2, name: "method", kind: "scalar", T: ScalarType.UINT32 },
    ] satisfies readonly PartialFieldInfo[],
    packedByDefault: true,
});

