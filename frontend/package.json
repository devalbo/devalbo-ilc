{
  "name": "dlc-web",
  "private": true,
  "type": "module",
  "description": "dlc's web tier (B3): React UI driving the same engine the CLI links, as a wasip2 component under jco with its WASI root bound to OPFS.",
  "scripts": {
    "dev": "cd .. && make dev-web",
    "build": "cd .. && make build-wasm && cd frontend && vite build",
    "preview": "vite preview",
    "test": "cd .. && make verify-web",
    "vite": "vite"
  },
  "dependencies": {
    "@aptre/protobuf-es-lite": "^1.1.1",
    "comlink": "^4.4.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@bytecodealliance/preview2-shim": "^0.19.0",
    "@playwright/test": "^1.54.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^7.0.5"
  }
}
