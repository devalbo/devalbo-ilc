{
  "name": "notes-web",
  "private": true,
  "type": "module",
  "description": "notes's web tier — the same engine the CLI links, running as a wasip2 component with its filesystem bound to OPFS.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "dependencies": {
    "@aptre/protobuf-es-lite": "^1.1.1",
    "comlink": "^4.4.2",
    "@devalbo/dlc-web": "file:../../../../dlc-platform/web"
  },
  "devDependencies": {
    "@bytecodealliance/preview2-shim": "^0.19.0",
    "typescript": "^5.6.3",
    "vite": "^7.0.5",
    "@playwright/test": "^1.54.1"
  }
}
