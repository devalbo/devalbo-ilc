version: v2
# devalbo/options/v1/options.proto is VENDORED here (see the note in that file):
# it defines method_id and the arg metadata options, and it lives in the ILC
# platform repo. It moves to a registry dep once published.
deps:
  - buf.build/protocolbuffers/wellknowntypes
modules:
  - path: .
lint:
  use:
    - STANDARD
breaking:
  use:
    - WIRE_JSON
